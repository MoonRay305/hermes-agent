# Phase 0 shadow-loop dry run (scratch)

This is a throwaway docs file used to exercise the autonomous build -> review
-> merge loop in SHADOW mode. It changes no product behavior. The shadow
orchestrator will review, run the eligibility verifier, and post a
"WOULD AUTO-MERGE" or "WOULD ESCALATE" alert -- it never merges.

Delete this file and its PR after the dry run.
